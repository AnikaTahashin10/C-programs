	#include <math.h>
	#include <stdio.h>

	int main() {
		float number, squareRoot;
		int square, num;

		printf("Enter a number : ");
		scanf_s("%d", &number);
		squareRoot = sqrt(number);
			printf("Square root of %d =  %d", number, squareRoot);
			square = num * num;
		printf("square of a given number %d is  =  %d", num, square);
	}